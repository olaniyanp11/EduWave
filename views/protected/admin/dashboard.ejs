<%- include('../../partials/header') %>

<section class="dashboard flex min-h-screen bg-gray-100">
  <!-- Sidebar -->
  <div class="w-[20%] bg-green-800 text-white flex flex-col justify-between p-6 relative">
    <!-- Toggle Button -->
    <button id="toggle-sidebar" class="bg-gray-900 p-2 rounded-r absolute top-1/2 right-0 transform -translate-y-1/2 z-50">
      <i class="fas fa-chevron-left"></i>
    </button>

    <div>
      <h2 class="text-xl font-semibold mb-6">Welcome, <span class="font-bold">Admin</span></h2>
      <ul class="space-y-4 text-sm">
        <li><a href="/admin/dashboard" class="hover:text-green-300"><i class="fas fa-chart-line"></i> Dashboard</a></li>
        <li><a href="/admin/users" class="hover:text-green-300"><i class="fas fa-users-cog"></i> Manage Users</a></li>
        <li><a href="/admin/courses" class="hover:text-green-300"><i class="fas fa-book"></i> Manage Courses</a></li>
        <li><a href="/" class="hover:text-green-300"><i class="fas fa-home"></i> Home</a></li>
      </ul>
    </div>

    <a href="/logout" class="text-sm mt-10 text-red-300 hover:text-red-500"><i class="fas fa-sign-out-alt"></i> Logout</a>
  </div>

  <!-- Main Content -->
  <main class="w-[80%] p-6">
    <div class="max-w-6xl mx-auto">
      <h1 class="text-3xl font-bold mb-6 text-green-700">Admin Dashboard</h1>

      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        <div class="bg-white p-6 rounded shadow">
          <h2 class="text-lg font-semibold text-gray-600">Total Users</h2>
          <p class="text-3xl font-bold text-green-700 mt-2"><%= stats.totalUsers %></p>
        </div>
        <div class="bg-white p-6 rounded shadow">
          <h2 class="text-lg font-semibold text-gray-600">Students</h2>
          <p class="text-3xl font-bold text-green-700 mt-2"><%= stats.totalStudents %></p>
        </div>
        <div class="bg-white p-6 rounded shadow">
          <h2 class="text-lg font-semibold text-gray-600">Instructors</h2>
          <p class="text-3xl font-bold text-green-700 mt-2"><%= stats.totalInstructors %></p>
        </div>
        <div class="bg-white p-6 rounded shadow">
          <h2 class="text-lg font-semibold text-gray-600">Courses</h2>
          <p class="text-3xl font-bold text-green-700 mt-2"><%= stats.totalCourses %></p>
        </div>
      </div>
    </div>
  </main>
</section>

<!-- Sidebar Toggle Script -->
<script>
  const toggleBtn = document.getElementById('toggle-sidebar');
  const sidebar = toggleBtn.parentElement;

  toggleBtn.addEventListener('click', () => {
    sidebar.classList.toggle('w-0');
    sidebar.classList.toggle('w-[20%]');
  });
</script>

<%- include('../../partials/footer') %>
